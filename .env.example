# Database
# SQLite database URL for Prisma
# In production, you may want to use PostgreSQL or another database
DATABASE_URL="file:./dev.db"

# Next.js
# Application environment (development, production, test)
NODE_ENV="development"

# Next.js Public URL (for absolute URLs in API routes)
# Set to your production URL in production: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# PayPal Integration
# PayPal Client ID (from PayPal Developer Dashboard)
# Get your credentials from: https://developer.paypal.com/dashboard/applications
PAYPAL_CLIENT_ID="your-paypal-client-id"
PAYPAL_CLIENT_SECRET="your-paypal-client-secret"
# PayPal environment: sandbox or production
PAYPAL_ENVIRONMENT="sandbox"

# Google Calendar API
# Google Calendar API credentials for multi-calendar integration
# Get your credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CALENDAR_CLIENT_ID="your-google-client-id"
GOOGLE_CALENDAR_CLIENT_SECRET="your-google-client-secret"
GOOGLE_CALENDAR_REDIRECT_URI="http://localhost:3000/api/auth/google/callback"
# Google Calendar IDs to aggregate (comma-separated)
GOOGLE_CALENDAR_IDS="calendar-id-1,calendar-id-2"

# API Keys & Secrets
# Application secret key for session management and encryption
# Generate a secure random string for production
NEXTAUTH_SECRET="your-nextauth-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Optional: Email Configuration
# For sending notifications and emails
# SMTP_MAIL_HOST="smtp.example.com"
# SMTP_MAIL_PORT="587"
# SMTP_MAIL_USER="your-email@example.com"
# SMTP_MAIL_PASSWORD="your-email-password"
